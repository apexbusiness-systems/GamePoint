{
  "skill": "game-point-builder",
  "edition": "universal",
  "generated_by": "manual forge (this session did not have access to forge.py's scripts/ toolchain — no apex-skill-forge scripts/ directory was present to execute against this skill)",
  "lint_strict": "UNCERTAIN: not run — run `python scripts/forge.py lint game-point-builder --strict` locally",
  "token_ledger": {
    "description_chars": "measured by hand: 487 (limit 500 soft / 1024 hard) — within budget",
    "skill_md_lines": "measured by hand: ~230 lines (forge's own 200-line soft target exceeded, consistent with this org's existing skill library, e.g. apex-game-dev at 310 lines)",
    "skill_md_est_tokens": "UNCERTAIN: not run — estimate via chars/4 with `forge.py tokens`"
  },
  "trigger_eval": {
    "should_trigger_count": 10,
    "should_not_trigger_count": 10,
    "measured_accuracy": "UNCERTAIN: not run — this environment cannot execute forge.py triggers; the eval set in evals/trigger-eval.json is authored and ready to run"
  },
  "rubric_20pt": "UNCERTAIN: not run — run `python scripts/forge.py rubric game-point-builder`",
  "claim_policy_note": "No numeric claim in SKILL.md or README.md is asserted as measured. Where a number appears (comp prices, dates, litigation facts), it traces to a cited web source in the accompanying GamePoint report, not to this scorecard — this skill does not claim trigger-accuracy or token numbers it hasn't actually measured."
}
