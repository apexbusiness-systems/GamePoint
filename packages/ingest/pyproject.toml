[project]
name = "gamepoint-ingest"
version = "1.0.0"
description = "GamePoint knowledge ingestion pipeline (WP-3): licensed sources -> synthesized claims -> embeddings."
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
